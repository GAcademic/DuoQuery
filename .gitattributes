# Normaliza los finales de línea en el repositorio (siempre LF en el objeto Git),
# independientemente de si el checkout es Windows o Unix.
* text=auto eol=lf

# Binarios explícitos, por si se añaden en el futuro
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
